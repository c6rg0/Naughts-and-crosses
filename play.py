import pygame
import sys

pygame.init()
window = pygame.display.set_mode((900,900))

while run == True:
    for event in pygame.event.get()
    if event.type == pygame.QUIT:
        run = False
    
    
